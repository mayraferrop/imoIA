name = "imoia-creatives"
main = "src/index.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]
account_id = "0c0849cb588d97a81801c98d389394d9"

# Workers Paid: eleva o limite default de 50ms para 30s.
# Satori + RESVG precisam de ~600ms em cold start (init WASM + render).
[limits]
cpu_ms = 30000

# CREATIVES_SECRET deve ser definido via: wrangler secret put CREATIVES_SECRET
# Não colocar aqui (fica no repo).
