{
  "name": "imoia-worker-creatives",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker que renderiza criativos M7 via @vercel/og (Satori). Consumido pelo backend Python.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "workers-og": "^0.0.27"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241011.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
